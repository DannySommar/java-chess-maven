package chess.core.move;

public class IlVaticanoMove extends Move
{
    public IlVaticanoMove(int fromFile, int fromRank, int toFile, int toRank)
    {
        super(fromFile, fromRank, toFile, toRank);
    }
    
}
