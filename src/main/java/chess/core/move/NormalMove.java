package chess.core.move;

public class NormalMove extends Move
{
    public NormalMove(int fromFile, int fromRank, int toFile, int toRank)
    {
        super(fromFile, fromRank, toFile, toRank);
    }
    
}
